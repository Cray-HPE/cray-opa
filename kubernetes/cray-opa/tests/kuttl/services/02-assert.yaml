apiVersion: v1
kind: Service
metadata:
  annotations:
    meta.helm.sh/release-name: cray-opa
    meta.helm.sh/release-namespace: opa
  name: ingressgateway-customer-user
  namespace: opa
spec:
  ports:
    - name: http
      port: 9191
      protocol: TCP
      targetPort: http
  sessionAffinity: None
  type: ClusterIP
